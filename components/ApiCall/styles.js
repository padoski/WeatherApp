import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 36,
  },
  input: {
    height: 40,
    margin: 12,
    borderWidth: 1,
  },
});
export default styles;
